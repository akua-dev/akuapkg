apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-vendored
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
    vendored: "true"
data:
  message: {{ .Values.message | quote }}
